DATABASE_URL="postgresql://tripbook:tripbook@localhost:55432/tripbook?schema=public"
# Generate with: node scripts/hash-password.mjs "your password"
# (paste the printed line as-is — the $ characters must stay escaped)
ADMIN_PASSWORD_HASH="\$2b\$12\$..."
